# Round 2 — main ring-in / ring-free states — verdict: FIX (one P1)

## P1
1. Purple (ring-free) arc drawn ~half the stroke of green in every state; joints step at 12 and 9 o'clock; in RING FREE the active phase is the thin one. → one stroke width for both (green width); if thickness carries meaning, tie it to current phase (full) vs other (~60%), not colour.

## P2
2. Hero block ~8–10 px right of the axis on all frames → measure digits + gap + unit as one group and centre it.
3. Today marker protrudes ~3 px beyond arc outer edge at 9 o'clock → marker radius ≤ arc half-width + 1 px, or pull arc centreline in 4 px.
4. Hero size doesn't scale to 454 (dead space) → one font tier up on 51 mm or centre block vertically (sits ~6 px low on 47/51).
5. Under 24 h still shows absolute date → one line `Remove · Today 2:26 AM` / `Tomorrow 12:26 AM`; two-line form only for the day tier.
6. Date→time leading too tight (~5 px) → 8–10 px or time one step smaller.
