# Round 2 — temporary-out + stress — verdict: SHIP (P2 only)

1. Over-run tail nearly invisible on red ring → contrasting colour (white or amber) with 2 px black gap, or thinner inner arc.
2. RING OUT under-limit block top-anchored, bottom 36% empty; jumps vs over-limit. → fill 5th row with `Reinsert by 12:36 PM` (white) so rows stay stable when flipping to "Reinsert now".
3. Over-limit stack lacks grouping → +14 px gap above `Reinsert now`, or move `Out since` last.
4. Arc stroke differs between states (RING OUT ~20 px vs RING IN ~12 px) → one stroke width.
5. Marker dot within ~3 px of display edge → centre dot on arc centreline with radius ≤ half stroke + 2 px, or inset arc ≥6 px.
