# Edit dates (native menu rows)

## P1
1. **Rows show no values.** Fix: Menu2 sub-labels with the value: `Inserted` / `Tue 8 Sep · 22:15`; `Removed` / `Tue 29 Sep · 22:15`; device 12/24 h; no year.
2. **"Removal" editable when no removal exists** — risks creating a removal by mistake. Fix: omit the row, or non-editable status row `Removed` / `Not yet — scheduled Tue 29 Sep`; backfilling is a separate explicit "Log removal…" action.

## P2
3. Labels are nouns → past-tense `Inserted` / `Removed`; no speculative "Next insertion" row.
4. Title "Edit dates" undersells (times matter; reminders move) → `Correct dates` (or `Edit ring dates`).
5. **Bottom row clipped by bezel** ("R" falls into the round cut-off). Fix: stock Menu2 heights, or cap to two rows in the safe band (second baseline ~y=300 at 416).

## P3
6. Initial focus on the most recent event (Inserted during ring-in, Removed during ring-out).
7. No chevrons needed once sub-labels exist.

**Verdict:** currently a table of contents, not an edit screen. Put values on rows, lock Removed until it exists, rename, fix clipping.
