# Main screen — serious states (ring-free >7 d, ring in >28 d, warning-wrap) 42/47/51 mm

## P1
1. **Ring-in >28 d is a green "all fine" screen with a red footnote.** Green arc/header/hero "6d 0h" contradicts "Replace now". Fix: serious state owns the frame — red full arc, header **REPLACE NOW**, red hero with exceeded value ("29d in" / "4w 1d in"), date line "Was due · Sun 13 Sep" in white; drop the countdown; warning shrinks to one line ("Use backup 7 days." if applicable).
2. **Ring-free hero "1d late" undersized and typographically inconsistent** with ring-in numerals. Fix: one hero style — big numerals + small unit (`1d` big, `late` small) at the same point size as ring-in.
3. **"INSERT NOW" header + "Insert now." warning line duplicate.** Fix: warning is one line "Use backup 7 days."; header carries the imperative. Ring-in: header "REPLACE NOW", line "Over 4 weeks · use backup."

## P2
4. **Inverted leading**: date/time crammed (2–4 px), warning lines airy (~12 px). Fix: date/time gap 6–8 px; warning gap 2–4 px or single line; or fold time into the date row.
5. **Copy ambiguity**: "Use backup 7 days." → "Backup contraception 7 days" (42 mm: "Backup 7 days"); "Ring in over 4 weeks. Replace now." → header REPLACE NOW + "Worn 29 days · backup 7 days".
6. **Arc semantics wrong in late state**: 75% fill + grey remainder reads "in progress"; cap at 12 collides with header "I" on 47/51. Fix: late ⇒ 100% red arc; move arc start to 6 o'clock or lower header; drop purple wedge + dot in serious states.
7. **Date row at edge of safe width on 42 mm.** Fix: drop "Remove ·" prefix (header states the action) or "20 Sep" without weekday on 42 mm.

## P3
8. **warning-wrap screenshots are identical to ring-free-over7d** — the wrap case isn't actually exercised; capture a deliberately long string at 42 mm.
9. Grey time on 42 mm below comfortable contrast → #AAA or fold into the date row.
10. Hero, header, warning all the same red → hero full red, warning quieter (darker red or white).
11. Vertical padding should scale with radius (42 vs 51 mm clearances differ).

**Verdict:** ring-free >7 d acceptable but redundant/undersized hero; ring-in >28 d fails the brief. Make it own the frame in red, delete duplicated "Insert now.", collapse warning to one line.
