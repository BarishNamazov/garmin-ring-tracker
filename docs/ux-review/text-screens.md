# First run / About / Regimen ("Schedule") / Migration notice

## P1
1. **Migration notice is a dead end** (no rail, no action, half screen empty). Fix: centre block; single action in the standard slot. Copy: **Dates updated** / *Your schedule now uses the real dates you inserted and removed the ring. Review them under "Edit dates".* / **OK**
2. **"Continue" indistinguishable from body copy.** One action treatment everywhere: fixed bottom slot (~y=330), accent colour, regular weight, selection brackets only when focused. First run: **I understand**.
3. **Regimen screen confused** (title "Schedule", button "Confirm", three indicators). Fix: title **Regimen**, subtitle *NuvaRing*, rows *In · 21 days / Out · 7 days*; if informational drop Confirm (Back exits); if a real step, title **Confirm regimen** with one action; remove rail (two rows don't scroll).

## P2
4. Rail inconsistent/lying → render only on overflow; same x (~374), thumb ≥4 px, proportional.
5. Everything bold → body regular ~26–28 px, leading 1.35, bold title only; first-run last line too close to the curve.
6. Copy telegraphic, poor rag → one calm paragraph: **Ring Tracker** / *Reminders for your ring schedule only. Not medical advice, and no measure of contraceptive protection.* / **I understand**
7. About repeats disclaimer, buries version → **About** / *Ring Tracker 1.2.0* (muted, smaller) / *Schedule reminders only. Not medical advice.*; full text below the fold if needed.

## P3
8. One subtitle style (muted ~24 px, 6 px under title) on all four.
9. Title→body 28–32 px; paragraph gap 14–16 px.
10. Migration: quote menu names or reword ("under Edit dates"); consider a banner instead of a full screen.

**Verdict:** bones right; fix migration dead end and action-slot treatment, then a typographic pass.
