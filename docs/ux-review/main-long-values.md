# Main screen stress cases (12 h long, 24 h, largest countdown), 42/47/51 mm

## P1
1. **Date line one string away from touching the ring** (~77% of dial width, 14–19 px clearance). Fix: max-width budget ~70% of chord; degrade: drop "Remove ·" (see 3) → drop weekday → step font down. Never within 24 px of the ring on 390 px.

## P2
2. **White date line outranks the countdown.** Fix: date regular weight / 70% grey; only countdown in full green; "RING IN" as dim grey eyebrow.
3. **"Remove ·" is a label inside the data line.** Fix: promote "Remove" to a small grey caps eyebrow above the date, or grey prefix + white date on one line; then time moves up tight under the date as one block.
4. **Countdown optically right-shifted** (~8–13 px) — trailing "h" counted as full width. Fix: centre on measured ink of numerals+units or hang units symmetrically.
5. **Top-heavy rhythm** (label→digits 47 px > digits→date 30 px; block 13 px high; bottom third empty). Fix: label→digits ~30 px, block down ~10 px, together with 1/3.

## P3
6. "d"/"h" grouping: word-space ~0.4 em between "d" and hour digits.
7. Time line under-weighted → one size step up; 12 h "PM" small caps; or fold into date line.
8. Across sizes consistent; ring stroke near-constant in px reads heavier on 42 mm.

**Verdict:** nothing clips today, but the date line is a latent clip. Fix the width budget and pull "Remove" out first; then dim date/eyebrow and re-centre digits.
